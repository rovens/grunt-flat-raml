{
  "name": "grunt-flat-raml",
  "version": "0.0.1",
  "description": "A grunt task to flatten multiple RAML files into a single one.",
  "main": "lib/flatten.js",
  "engines": {
    "node": ">= 0.8.0"
  },
  "scripts": {
    "test": "grunt test"
  },
  "author": "",
  "license": "ISC",
  "readmeFilename": "README.md",
  "dependencies": {
    "flat-raml": "~1.2.1",
    "grunt": "~0.4.5"
  }
}
